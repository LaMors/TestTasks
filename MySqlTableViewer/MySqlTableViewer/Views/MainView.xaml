<Window x:Class="MySqlTableViewer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MySqlTableViewer.Views"
        mc:Ignorable="d"
        Title="Tables Viewer" Height="450" Width="800">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Grid>
        <DataGrid ItemsSource="{Binding Tables}" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  ColumnHeaderHeight="40"
                  Padding="5"
                  RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Database name:" Binding="{Binding DatabaseName}"  />
                <DataGridTextColumn Header="Table:" Binding="{Binding TableName}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
